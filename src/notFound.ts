/**
 * Точка входа страницы 404 (Figma, node 332:508).
 *
 * Разметка статическая и лежит в 404.html — здесь только стили. Отдельная
 * точка входа, а не main.tsx: тащить на страницу с тремя строками текста весь
 * React с приложением незачем, тем более что попадают на неё по ошибке и
 * уходят сразу.
 */

import './styles/base.css';
import './styles/preloader.css';
